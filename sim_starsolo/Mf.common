SHELL=/bin/bash
thisFile := $(lastword $(MAKEFILE_LIST))
dd:=/home/dsullivan/benchmarking/starsolo/STARsoloManuscript/
timer:=/bin/time -v

# command to run before every recipe
#prepComm := set -o history -o histexpand
prepComm := set -v

thisFile := $(lastword $(MAKEFILE_LIST))

.SUFFIXES: #no implicit rules for 

